# Replaced by Supabase Edge Functions.

The Cloudflare Worker in this folder is obsolete. Do not deploy it.

Use `supabase/functions/` plus `supabase/migrations/` instead. Historical Worker/KV/Apps Script orders are not imported automatically — see docs/supabase.md.

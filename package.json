{
  "name": "ld-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && python3 -m http.server 3000",
    "test": "node --test js/utils/*.test.js js/checkout/*.test.js test/*.test.js",
    "supabase:functions": "npx --yes supabase functions serve --no-verify-jwt"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.57.4"
  },
  "engines": {
    "node": ">=18"
  }
}
